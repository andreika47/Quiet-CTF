# n1fnv1
**N1CTF 2025**

В задании реализована кастомная 128-битная функция хэширования со случайно сгенерированными параметрами. Даны параметры и случайно 128-битное число, нужно подобрать сообщение, хэш которого будет равен этому числу.

```
import os, secrets, signal

def hash(params, msg):
    a, b, c, m, x = params
    for byte in msg:
        for bit in f'{byte:08b}':
            x = ((x * a + b + int(bit)) ^ c) % m
    return x

nbits = 128
rand = lambda: secrets.randbits(nbits)
print('⚙️', params := (rand() | 1, rand(), rand(), 2 ** nbits, rand()))
print('🎯', target := rand())

signal.alarm(900)
message = bytes.fromhex(input('💬 '))
assert hash(params, message) == target, '❌'
print('🚩', os.getenv("FLAG"))
```

Из генерации параметров следует, что `a` - нечетное число, `m = 2^128`. Из того, что `m` - большая степень двойки следует, что младшие биты (LSB) текущего состония хэширования `x_i` зависят только от текущего бита сообщения `msg_i` и LSB предыдущего состояния `x_i-1`. А так как `gcd(a, m) = 1`, значить, что мы может найти обратный элемент к `a`, что поможет нам для `x_i` найти предыдущее состояние `x_i-1`.

Попробуем найти такое `msg`, чтобы 32 LSB соответствовали 32 LSB `target`. Для этого будет использовать метод встречи посередине (MitM). 
Далее, нам нужно будет найти такое "расширение" найденного сообщение, которое при дальнейшем хэшировании не будет изменять LSB. То есть, нам нужно найти такое `ext1msg = ext1 + msg` (тут `+` означает конктаенацию), чтобы `hash(ext1msg) = hash(msg) mod 2^32`, то есть последние 32 бита хэша от расишренного и найденного на предыдущем шаги сообщений совпадают. На последнем шаге у нас получатся достаточно длинные возможные сообщений (самое маленькое, что я находил было примерно 4000 байт).

Так как
`x_i = ((x_i-1 * a + b + msg_i) ^ c) mod 2^128`,
то
`x_i-1 = (((x_i ^ c) - msg_i - b) * a_inv) mod 2^128`,
где `a_inv = a ^ -1 mod 2^128`.

В реализации MitM будем вычислять все возможные состояния текущего состояния после некоторого `k` шагов (прямой проход) и некоторое `r` шагов от целевого состояния. Оптимальное количество шагов заранее сказать сложно, поэтому будем перебирать все размеры шагов (в решении перебираем от 8 до 12 шагов). Если найдено достаточное количество путей, то останавливаемся. При этом количество возможных путей тоже лучше ограничить, чтобы перебор не разрастался. Поэтому решим, что достаточное количество путей - оно же максимальное (в решении ограничиваемся `16` путями).

Из-за подобных оптимизаций для некоторых `target` решение может не найтись. Поэтому просто запускаем код решения еще раз

#crypto #hash #mitm